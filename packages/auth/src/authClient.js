// authClient.js - Cliente de autenticación Supabase
// Será extraído de /assets/js/auth/authClient.js en Fase 2

// Placeholder - Se implementará en Fase 2
export const authClient = {
  // Métodos que se migrarán:
  // - getCurrentUser()
  // - login(email, password)
  // - register(email, password, userData)
  // - logout()
  // - resetPassword(email)
  // - updateProfile(userData)
};
